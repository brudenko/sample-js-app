{
  "name": "simple-web-js-app",
  "version": "1.0.0",
  "scripts": {
    "start": "npx serve dist",
    "build": "esbuild src/main.js --bundle --outfile=dist/bundle.js",
    "test": "jest"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "jest": "^29.0.0",
    "serve": "^14.2.0"
  },
  "type": "module"
}
